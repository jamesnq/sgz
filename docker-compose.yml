version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    restart: always
    networks:
      - dokploy-network
networks:
  dokploy-network:
    external: true
